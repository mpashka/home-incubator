Создай новый модуль fe_mobile_android. Это приложение для Android. Версия android - 15.
OS - Xiaomi HyperOS 2. Приложение должно уметь сканировать QR код чеков и отправлять
результат по определённому URL. QR код большой (около 900 байт), размер QR кода -
4х4 см, поэтому качество QR сканера критично.
---
# Task: Создание Android модуля fe_mobile_android

## Требования
- Версия android: 15
- OS: Xiaomi HyperOS 2
- Сканирование QR кодов чеков (~900 байт)
- Отправка на backend

## Статус: ВЫПОЛНЕНО

## Реализация

Создан модуль `fe_mobile_android/` со следующей функциональностью:

### Технологии
- **Kotlin** - основной язык
- **ML Kit Barcode Scanning** - распознавание QR кодов (оптимизирован для больших QR)
- **CameraX** - работа с камерой
- **Retrofit + OkHttp** - HTTP клиент
- **DataStore** - хранение настроек
- **Material Design 3** - UI

### Функционал
1. Сканирование QR кодов чеков
2. Автоматическая отправка на backend
3. Настройка URL backend'а и User ID
4. История сканирований
5. Вибрация при успешном сканировании
6. Фильтрация только чековых QR кодов (tax.gov, verifyInvoice)

### Структура
```
fe_mobile_android/
├── app/src/main/java/com/receipt/scanner/
│   ├── api/          # API клиент
│   ├── data/         # Репозитории
│   ├── ui/           # Activities
│   └── util/         # QR анализатор
├── gradle/libs.versions.toml
└── build.gradle.kts
```

### Сборка
```bash
cd fe_mobile_android
./gradlew assembleDebug
```

Документация: `@/docs/fe_mobile_android/overview.md`

---
В модуль fe_mobile_android необходимо добавить новый функционал:
1) Новый функционал должен быть написан на Java 17 - с использованием
   var, records, text blocks
2) Приложение должно быть рассчитано на минимальную версию андроид - 14
3) На главном экране есть кнопка "Scan History". Необходимо добавить две кнопки:
    * сканирование QR кода (текущий функционал)
    * сканирование текста чека
4) В режиме сканирования чека должно искать и сканировать три строчки текста:
```
========================================
ПФР време:          07.09.2024. 19:25:47
ПФР број рачуна: P8BPS55R-P8BPS55R-71822
Бројач рачуна:             64763/71822ПП
========================================
```
Необходимо отправлять содержимое этих трёх строк на бэкенд:
время, номер чека и счётчик (Бројач)
5) Для хранения этой информации на бэкенде необходимо добавить новую таблицу
6) На бэкенд необходимо добавить API, которое принимает и хранит эту информацию.  
   функционал по обработке данных на бэкенд добавлять не надо.
7) Обнови документацию. В том числе ER диаграммы, бэк, mobile_android

---
не существует пакета com.google.mlkit:text-recognition-cyrillic для offline распознавания кириллических текстов. Текст чека смешанный - надписи выполнены        
кирилицей, номер чека - латиницей. Можно использовать ml kit для анализа структуры текста. Сделай две имплементации распознавания текста. Первая имплементация   
пусть использует mlkit offline latin для распознавания символов. Вторая имплементация пусть использует Firebase ML для полноценного распознавания кириллического
текста надписей и латинского текста номера чека.
