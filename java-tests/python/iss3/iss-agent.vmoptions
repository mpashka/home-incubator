-d64
-server
-Xmx256m
-Xss384k
# Used for extended metrics only to load agent at runtime
-Xbootclasspath/a:jre/lib/tools.jar
-XX:+AlwaysPreTouch
-XX:-CICompilerCountPerCPU
# A bit conservative. Maybe can be reduced to 1
-XX:CICompilerCount=2
-XX:MetaspaceSize=128m
-XX:MaxMetaspaceSize=128m
# ISS-5116: max value ~76m using stress suite
-XX:ReservedCodeCacheSize=96m
-XX:CompressedClassSpaceSize=32m
-XX:+UseCodeCacheFlushing
-XX:+UseG1GC
-XX:ParallelGCThreads=4
# http://www.evanjones.ca/jvm-mmap-pause.html
-XX:+PerfDisableSharedMem
-XX:+HeapDumpOnOutOfMemoryError
-XX:OnOutOfMemoryError="/db/iss3/oom-killer.sh %p"
-XX:HeapDumpPath="/db/iss3/java_pid_singleton.hprof"
-Dsun.net.inetaddr.ttl=10
-Dsun.net.inetaddr.negative.ttl=10
-XX:ReservedCodeCacheSize=80m
-XX:CodeCacheMinimumFreeSpace=10m
-XX:-TieredCompilation
# Number of times to use the JNI accessor before the JVM changes to use the Java bytecode accessor.
# Due to large amount of reflective calls we considered to not to pollute heap with additional
# generated bytecode/classloaders, but turns out that it's better to investigate java heap dump instead
# of coredump caused by oom_killer. Keeping this option for sake of future investigations.
# -Dsun.reflect.inflationThreshold=2147483647
-Djava.net.preferIPv6Addresses=true
-Dagent.webapp.port=25536
-Dagent.stdout.suppress=true
# Debugger port
-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=localhost:25538
-Dakka.loglevel=INFO
-Dremote.host=iss-ci-vm-5.sas.yp-c.yandex.net
-Dagent.shardtracker.host=iss-ci-vm-5.sas.yp-c.yandex.net
-Dagent.root=./
-Dagent.autoUpdatePeriod=1s
-Dagent.autoSyncPeriod=1s
-Dagent.feedback.resendPeriod=1s
#-- start managed zone --
-XX:+UnlockDiagnosticVMOptions
-XX:-UseBiasedLocking
-XX:GuaranteedSafepointInterval=300000
#-- end managed zone --
