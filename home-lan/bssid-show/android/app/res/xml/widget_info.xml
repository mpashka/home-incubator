<?xml version="1.0" encoding="utf-8"?>
<!--
  updatePeriodMillis нужен не для чтения Wi-Fi (в фоне BSSID не отдаётся), а чтобы виджет
  сам погасил иконку, когда показания устарели. Читаем только по нажатию — см. RefreshActivity.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="40dp"
    android:minHeight="40dp"
    android:targetCellWidth="1"
    android:targetCellHeight="1"
    android:updatePeriodMillis="1800000"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:previewLayout="@layout/widget"
    android:initialLayout="@layout/widget"
    android:description="@string/widget_desc" />
